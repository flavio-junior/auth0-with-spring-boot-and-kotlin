security:
  jwt:
    token:
      secret-key:  53cr37
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/auth
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true